# Keep the local map lightweight. Static walls are handled by the global map;
# runtime global-plan subgoals keep TEB from cutting across corners.
footprint: [[0.11, 0.10], [0.11, -0.10], [-0.11, -0.10], [-0.11, 0.10]]
footprint_padding: 0.01

obstacle_range: 2.8
raytrace_range: 3.2
transform_tolerance: 0.5

inflation_radius: 0.20
cost_scaling_factor: 2.8

plugins:
  - {name: obstacle_layer, type: "costmap_2d::ObstacleLayer"}
  - {name: inflation_layer, type: "costmap_2d::InflationLayer"}

obstacle_layer:
  observation_sources: laser_scan_sensor
  laser_scan_sensor:
    sensor_frame: base_scan
    data_type: LaserScan
    topic: /scan_filtered
    marking: false
    clearing: true
    expected_update_rate: 0.0
    inf_is_valid: true
